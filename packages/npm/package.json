{
  "name": "curlpit",
  "version": "0.3.0",
  "description": "Terminal-first HTTP runner for replaying scripted requests.",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/curlpit-sh/cli.git"
  },
  "bin": {
    "curlpit": "dist/run.js",
    "crlp": "dist/run.js"
  },
  "scripts": {
    "build": "rm -rf dist && bun build ./src/install.ts ./src/run.ts --outdir dist --target node --format esm",
    "postinstall": "node dist/install.js"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist",
    "vendor",
    "README.md",
    "LICENSE"
  ],
  "dependencies": {
    "@curlpit/scripts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "typescript": "^5.5.4"
  },
  "keywords": [
    "curl",
    "http",
    "cli",
    "terminal",
    "rest",
    "api"
  ]
}
